
function perimetroAreaDeUmCirculo (raio) {
const comprimento = 2 * Math.PI * raio;
const area = Math.PI * raio ** 2
console.log(`O comprimento é ${comprimento.toFixed(2)}`);
console.log(`A area é ${area.toFixed(2)}`);
	
}
perimetroAreaDeUmCirculo(2)